// This component has been removed as the application no longer supports multiple languages
// and all content is now aligned Left-to-Right (LTR) for English

// Keeping as placeholder to prevent import errors
export function LanguageSelector() {
  return null;
}
