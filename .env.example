
# DBooster Environment Variables
# SECURITY NOTE: This file contains example values only. Never commit real secrets to version control.

# Database Configuration
# Replace with your actual database URL - keep this secret
DATABASE_URL=postgresql://username:password@host:port/database

# Authentication Secrets
# These are sensitive - never commit real values
AUTH_SECRET=your_auth_secret_here_minimum_32_characters
AUTH_GITHUB_ID=your_github_client_id
AUTH_GITHUB_SECRET=your_github_client_secret
AUTH_GOOGLE_ID=your_google_client_id
AUTH_GOOGLE_SECRET=your_google_client_secret

# API Keys (Keep these secret)
OPENAI_API_KEY=sk-your_openai_api_key_here
GITHUB_TOKEN=ghp_your_github_token_here

# App Configuration (Safe to set)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=DBooster
NEXT_PUBLIC_APP_DESCRIPTION="AI-Powered Database Query Optimizer"

# Optional Services (Keep secrets private)
GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX
MIXPANEL_TOKEN=your_mixpanel_token

# Email Configuration (Keep credentials private)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Security Settings
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=900000
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
CSP_REPORT_URI=/api/csp-report

# Feature Flags (Safe to modify)
FEATURE_DARK_MODE=true
FEATURE_I18N=true
FEATURE_PWA=true
FEATURE_ANALYTICS=false

# IMPORTANT SECURITY NOTES:
# 1. Never commit real API keys or secrets to version control
# 2. Use environment-specific configuration files for different environments
# 3. All secrets should be managed through secure secret management systems
# 4. Regularly rotate API keys and tokens
# 5. Use the minimum required permissions for each service
